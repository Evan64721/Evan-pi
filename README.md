# Evan-pi

Source for **evan.leckliter.net** — a static website served from a Raspberry Pi.

## Structure

- `index.html` — homepage
- `style.css` — styles

## Deploy

The website pulls the latest version from this repo:

```bash
git pull
```
